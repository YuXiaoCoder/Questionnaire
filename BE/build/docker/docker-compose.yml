version: "3"

networks:
  backend:
    driver: "bridge"

services:
  db:
    image: "mysql:5.7"
    container_name: "mysql"
    networks:
      - backend
    ports:
      - 3306:3306
    restart: "always"
    environment:
      MYSQL_ROOT_PASSWORD: "123456"
